# <img src="https://flagcdn.com/w20/kr.png" width="20" style="vertical-align: middle;"> JAVA AWS CI/CD 풀스택과정 - SpringBoot<br>
### <img src="https://flagcdn.com/w20/us.png" width="20" style="vertical-align: middle;"> JAVA AWS CI/CD Full-stack Course - SpringBoot<br>
### <img src="https://flagcdn.com/w20/jp.png" width="20" style="vertical-align: middle;"> JAVA AWS CI/CD フルスタック課程 - SpringBoot<br>

<br>
<br>

* **Progress** 
	<p>
		<img src="https://img.shields.io/badge/Progress-40%25-orange?style=flat-square" align="absmiddle">
* **Language & Environment** 
	<p>
		<img src="https://img.shields.io/badge/Java_21-ED8B00?style=flat-square&logo=openjdk&logoColor=white" align="absmiddle"> 
		<img src="https://img.shields.io/badge/JavaScript-F7DF1E?style=flat-square&logo=javascript&logoColor=black" align="absmiddle">
		<img src="https://img.shields.io/badge/Node.js-339933?style=flat-square&logo=nodedotjs&logoColor=white" align="absmiddle">
		<img src="https://img.shields.io/badge/HTML5-E34F26?style=flat-square&logo=html5&logoColor=white" align="absmiddle"> 
		<img src="https://img.shields.io/badge/CSS3-1572B6?style=flat-square&logo=css3&logoColor=white" align="absmiddle">
* **Framework & Library** 
	<p>
		<img src="https://img.shields.io/badge/Spring_Boot-6DB33F?style=flat-square&logo=springboot&logoColor=white" align="absmiddle">
		<img src="https://img.shields.io/badge/Thymeleaf-005F0F?style=flat-square&logo=thymeleaf&logoColor=white" align="absmiddle">
		<img src="https://img.shields.io/badge/Vue.js-4FC08D?style=flat-square&logo=vuedotjs&logoColor=white" align="absmiddle"> 
		<img src="https://img.shields.io/badge/jQuery-0769AD?style=flat-square&logo=jquery&logoColor=white" align="absmiddle">
* **Network & API** 
	<p>
		<img src="https://img.shields.io/badge/Axios-5A29E4?style=flat-square&logo=axios&logoColor=white" align="absmiddle">
* **DataBase** 
	<p>
		<img src="https://img.shields.io/badge/Oracle_21c-F80000?style=flat-square&logo=oracle&logoColor=white" align="absmiddle">
* **Server** 
	<p>
		<img src="https://img.shields.io/badge/Apache_Tomcat-F8DC75?style=flat-square&logo=apachetomcat&logoColor=black" align="absmiddle">
* **Build Tools** 
	<p>
		<img src="https://img.shields.io/badge/Gradle-02303A?style=flat-square&logo=gradle&logoColor=white" align="absmiddle">
* **IDE & Tools** 
	<p>
		<img src="https://img.shields.io/badge/Eclipse-2C2255?style=flat-square&logo=eclipseide&logoColor=white" align="absmiddle"> 
		<img src="https://img.shields.io/badge/VS_Code-007ACC?style=flat-square&logo=visualstudiocode&logoColor=white" align="absmiddle"> 
		<img src="https://img.shields.io/badge/SQL_Developer-F80000?style=flat-square&logo=oracle&logoColor=white" align="absmiddle"> 
		<img src="https://img.shields.io/badge/DBeaver-382923?style=flat-square&logo=dbeaver&logoColor=white" align="absmiddle">
		
<br>
<br>

📅 학습 일지 (Study Log / 学習記録)
------------------------------

<br>
자세한 내용: <a href="https://ki-sd.tistory.com/category/Spring-Boot" target="_blank"><img src="https://img.shields.io/badge/Tistory-000000?style=flat-square&logo=tistory&logoColor=white&link=https://ki-sd.tistory.com/"></a>
<br>

#### 2026-07
| 날짜 | 학습 내용 (KR / US / JP) |
| :--- | :--- |
| **07-27** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[Spring Boot 초기 환경 구축]</strong> Spring Boot 프로젝트를 생성하고 필요한 의존성(Dependencies) 라이브러리 세팅을 완료함. 자동 설정(AutoConfiguration) 기반의 컨테이너 구동 방식을 확인함. <br><strong>[View Template 연동 및 테스트]</strong> 기본 데이터 출력과 더불어, 서버 사이드 렌더링(SSR)을 위한 두 가지 뷰 템플릿(JSP, Thymeleaf)을 각각 연동함. 레거시 뷰(JSP)와 모던 템플릿 엔진(Thymeleaf)의 View Resolver 라우팅 및 화면 출력 동작을 대조하여 테스트함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Spring Boot Initialization]</strong> Set up a Spring Boot project and configured required dependency libraries. Verified the container startup process based on AutoConfiguration. <br><strong>[View Template Integration]</strong> Tested basic data outputs and integrated two distinct view templates (JSP, Thymeleaf) for Server-Side Rendering (SSR). Compared the View Resolver routing and output mechanics of the legacy JSP approach versus the modern Thymeleaf engine.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[Spring Bootの初期環境構築]</strong> Spring Bootプロジェクトを作成し、必要な依存ライブラリ(Dependencies)の設定を完了。自動設定(AutoConfiguration)に基づくコンテナ起動方式を確認。<br><strong>[View Templateの連動およびテスト]</strong> 基本的なデータ出力に加え、サーバーサイドレンダリング(SSR)のための2つのビューテンプレート(JSP, Thymeleaf)をそれぞれ連動。レガシービュー(JSP)とモダンテンプレートエンジン(Thymeleaf)のView Resolverルーティングおよび画面出力動作を対照してテスト。</li></ul> |
| **08-03** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[JPA 시퀀스 및 DTO 프로젝션]</strong> `BoardEntity`에 `@SequenceGenerator`와 `@GeneratedValue`를 설정하여 오라클 시퀀스(`jpb_no_seq`)를 매핑함. 또한 `BoardDTO` 인터페이스를 생성하여 네이티브 쿼리 결과를 엔티티가 아닌 DTO로 직접 프로젝션(Projection)받아 포맷팅된 날짜(`dbday`)를 추출함. <br><strong>[JPA 기반 CRUD 및 검증]</strong> `BoardServiceImpl`에서 `findById`로 기존 영속성 객체를 조회하고, 평문 비밀번호가 일치할 때만 `dao.save()`(수정) 또는 `dao.delete()`(삭제)를 수행하도록 비즈니스 로직을 구현함. <br><strong>[Thymeleaf 동적 제어]</strong> `@ModelAttribute`로 요청 폼 데이터를 수집하고, `delete_ok.html` 뷰 등에서 `th:if="${res=='no'}"`와 같은 조건문을 사용하여 컨트롤러 처리 결과에 따라 JavaScript 경고창 출력 및 리다이렉트 흐름을 동적으로 제어함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[JPA Sequences & DTO Projection]</strong> Mapped Oracle sequences (`jpb_no_seq`) in `BoardEntity` using `@SequenceGenerator` and `@GeneratedValue`. Created a `BoardDTO` interface to project native query results directly into a DTO to extract formatted dates (`dbday`). <br><strong>[JPA-based CRUD & Validation]</strong> Implemented business logic in `BoardServiceImpl` to fetch persistent objects via `findById`, performing `dao.save()` (update) or `dao.delete()` only when passwords match. <br><strong>[Thymeleaf Dynamic Control]</strong> Collected form data using `@ModelAttribute`, and utilized conditional statements like `th:if="${res=='no'}"` in `delete_ok.html` and others to dynamically execute JavaScript alerts or redirections based on processing results.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[JPAシーケンスとDTOプロジェクション]</strong> `BoardEntity`に`@SequenceGenerator`と`@GeneratedValue`を設定し、Oracleシーケンス（`jpb_no_seq`）をマッピング。また、`BoardDTO`インターフェースを作成し、ネイティブクエリの結果をエンティティではなくDTOに直接プロジェクション（Projection）してフォーマットされた日付（`dbday`）を抽出。<br><strong>[JPA基盤のCRUDと検証]</strong> `BoardServiceImpl`にて`findById`で既存の永続性オブジェクトを取得し、平文パスワードが一致する場合のみ`dao.save()`（修正）または`dao.delete()`（削除）を実行するビジネスロジックを実装。<br><strong>[Thymeleaf動的制御]</strong> `@ModelAttribute`でフォームデータを収集し、`delete_ok.html`ビュー等において`th:if="${res=='no'}"`などの条件文を用いて、処理結果に応じたJavaScriptの警告表示やリダイレクトを動的に制御。</li></ul> |
| **08-04** | <ul><li><a href="https://github.com/ki-sd/SpringThymeLeafFirstProject_2.git" target="_blank"><img src="https://img.shields.io/badge/GitHub-181717?style=flat-square&logo=github&logoColor=white"></a></li><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[JPA Paging & Service]</strong> `PageRequest`와 `Pageable`을 활용하여 페이징 및 정렬 조회 로직을 적용하고, `getTotalPages()`를 활용해 UI 블록 처리용 배열 연산을 서비스 계층에서 구현함. <br><strong>[AOP & Exception]</strong> `FoodAOP`에 `@Around`를 적용하여 모든 컨트롤러의 메서드 실행 시간 추적 로직을 구현하고, `@ControllerAdvice`를 통해 전역 예외 처리(Global Exception Handling) 체계를 구성함. <br><strong>[View & REST]</strong> Thymeleaf의 `#numbers.sequence`와 조건부 클래스 바인딩을 이용해 동적 페이징 UI를 구현함. 동시에 Vue 연동을 위해 `GoodsRestController`를 분리하고 `ResponseEntity` 기반의 JSON 응답을 구성함. <br><strong>[Docker 배포 실습]</strong> Ubuntu 환경에 Docker 엔진을 설치한 뒤, `eclipse-temurin:21-jdk` 베이스의 `Dockerfile`을 작성함. Spring Boot `.jar` 애플리케이션을 도커 이미지로 빌드하고, 개인 Docker Hub 저장소(`kisd09/springboot-app1`)에 Push 하는 컨테이너 배포 과정을 실습함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[JPA Paging & Service]</strong> Applied paging and sorting logic using `PageRequest` and `Pageable`, and implemented UI block array calculations in the service layer using `getTotalPages()`. <br><strong>[AOP & Exception]</strong> Implemented method execution time tracking for all controllers via `@Around` in `FoodAOP`, and established a global exception handling system using `@ControllerAdvice`. <br><strong>[View & REST]</strong> Built dynamic pagination UIs using Thymeleaf's `#numbers.sequence` and conditional class binding. Extracted `GoodsRestController` for Vue integration, providing JSON responses via `ResponseEntity`. <br><strong>[Docker Deployment Practice]</strong> Installed the Docker engine on Ubuntu and created a `Dockerfile` based on `eclipse-temurin:21-jdk`. Built the Spring Boot `.jar` application into a Docker image and pushed it to a personal Docker Hub repository (`kisd09/springboot-app1`).</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[JPA Paging & Service]</strong> `PageRequest`と`Pageable`を活用してページングおよびソートロジックを適用し、`getTotalPages()`を利用してUIブロック処理用の配列演算をサービス階層で実装。<br><strong>[AOP & Exception]</strong> `FoodAOP`に`@Around`を適用して全コントローラーのメソッド実行時間を追跡するロジックを実装し、`@ControllerAdvice`を通じてグローバル例外処理（Global Exception Handling）体系を構築。<br><strong>[View & REST]</strong> Thymeleafの`#numbers.sequence`と条件付きクラスバインディングを用いて動的なページングUIを実装。同時にVue連携のため`GoodsRestController`を分離し、`ResponseEntity`基盤のJSON応答を構成。<br><strong>[Dockerデプロイ実習]</strong> Ubuntu環境にDockerエンジンをインストールした後、`eclipse-temurin:21-jdk`ベースの`Dockerfile`を作成。Spring Bootの`.jar`アプリケーションをDockerイメージとしてビルドし、個人のDocker Hubリポジトリ（`kisd09/springboot-app1`）にPushするコンテナデプロイ過程を実習。</li></ul> |
| **08-05** | <ul><li><a href="https://github.com/ki-sd/SpringThymeLeafIncludeProject.git" target="_blank"><img src="https://img.shields.io/badge/GitHub-181717?style=flat-square&logo=github&logoColor=white"></a></li><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[JPA Native Query]</strong> `@Query(nativeQuery=true)`와 `OFFSET / FETCH NEXT` 구문을 활용해 오라클 DB 레벨에서 페이징을 최적화하고, 결과를 엔티티가 아닌 `VO` 객체로 직접 프로젝션함. <br><strong>[Service Pagination]</strong> 서비스 계층에서 데이터베이스의 총 레코드 수(`count()`)를 조회한 뒤 `Math.ceil`을 활용해 전체 페이지와 블록 단위 페이징(`startPage`, `endPage`) 연산 로직을 구현함. <br><strong>[REST API]</strong> 향후 Vue.js 프론트엔드 연동을 대비하여 `MainRestController`를 분리하고, `@CrossOrigin` 설정 및 `ResponseEntity` 기반의 JSON 응답 구조를 세팅함. <br><strong>[Thymeleaf UI]</strong> `#numbers.sequence` 디렉티브를 사용해 동적 페이징 네비게이션을 렌더링하고, `th:if` 조건문으로 상품의 할인가와 원가를 비교해 조건부로 할인율 UI를 노출하도록 뷰를 구성함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[JPA Native Query]</strong> Optimized pagination at the Oracle DB level using `@Query(nativeQuery=true)` and `OFFSET / FETCH NEXT` syntax, directly projecting results into `VO` objects instead of entities. <br><strong>[Service Pagination]</strong> Implemented logic in the service layer to calculate total pages and block-level pagination (`startPage`, `endPage`) using `Math.ceil` based on the total record count (`count()`). <br><strong>[REST API]</strong> Separated `MainRestController` to prepare for Vue.js integration, configuring `@CrossOrigin` and establishing a JSON response structure using `ResponseEntity`. <br><strong>[Thymeleaf UI]</strong> Rendered dynamic pagination navigation using the `#numbers.sequence` directive, and built views using `th:if` statements to conditionally display discount UIs by comparing original and discounted prices.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[JPA Native Query]</strong> `@Query(nativeQuery=true)`と`OFFSET / FETCH NEXT`構文を活用してOracle DBレベルでページングを最適化し、結果をエンティティではなく`VO`オブジェクトとして直接プロジェクション。<br><strong>[Service Pagination]</strong> サービス階層で総レコード数（`count()`）を照会した後、`Math.ceil`を活用して全体ページおよびブロック単位のページング（`startPage`、`endPage`）演算ロジックを実装。<br><strong>[REST API]</strong> 今後のVue.jsフロントエンド連携に備えて`MainRestController`を分離し、`@CrossOrigin`設定および`ResponseEntity`基盤のJSON応答構造を構築。<br><strong>[Thymeleaf UI]</strong> `#numbers.sequence`ディレクティブを使用して動的ページングナビゲーションをレンダリングし、`th:if`条件文で商品の割引価格と原価を比較して条件付きで割引率UIを表示するようにビューを構成。</li></ul> |
| **08-06** | <ul><li><a href="https://github.com/ki-sd/SpringThymeLeafDockerProject.git" target="_blank"><img src="https://img.shields.io/badge/GitHub-181717?style=flat-square&logo=github&logoColor=white"></a></li><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[JPA 기반 데이터 처리]</strong> `Recipe` 및 `Chef` 엔티티를 설계하고, `JpaRepository`와 `PageRequest`를 활용해 데이터베이스 연동 및 페이징 조회 로직을 객체지향적으로 구현함. <br><strong>[Thymeleaf 동적 뷰]</strong> 컨트롤러에서 전달된 페이징 데이터를 바탕으로 `#numbers.sequence`를 사용해 네비게이션을 렌더링하고, `th:each`로 레시피와 셰프 목록을 출력함. <br><strong>[Docker Compose CI/CD]</strong> Ubuntu 환경에서 Gradle로 프로젝트를 빌드한 뒤 도커 이미지를 생성하여 Docker Hub(`kisd09/recipe-app`)에 푸시함. 이후 `docker-compose.yml`을 작성해 `docker compose up -d`로 컨테이너 기반 배포를 실습함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[JPA Data Processing]</strong> Designed `Recipe` and `Chef` entities and implemented object-oriented database access and pagination using `JpaRepository` and `PageRequest`. <br><strong>[Thymeleaf Dynamic Views]</strong> Rendered navigation using `#numbers.sequence` based on pagination data from the controller, and displayed recipe and chef lists using `th:each`. <br><strong>[Docker Compose CI/CD]</strong> Built the project with Gradle in an Ubuntu environment, created a Docker image, and pushed it to Docker Hub (`kisd09/recipe-app`). Practiced container deployment by writing `docker-compose.yml` and running `docker compose up -d`.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[JPAデータ処理]</strong> `Recipe`および`Chef`エンティティを設計し、`JpaRepository`と`PageRequest`を活用してデータベース連携およびページング取得ロジックをオブジェクト指向的に実装。<br><strong>[Thymeleaf動的ビュー]</strong> コントローラーから渡されたページングデータをもとに、`#numbers.sequence`を使用してナビゲーションをレンダリングし、`th:each`でレシピとシェフのリストを出力。<br><strong>[Docker Compose CI/CD]</strong> Ubuntu環境にてGradleでプロジェクトをビルド後、Dockerイメージを作成しDocker Hub(`kisd09/recipe-app`)にプッシュ。その後`docker-compose.yml`を作成し、`docker compose up -d`でコンテナ基盤のデプロイを実習。</li></ul> |
| **08-07** | <ul><li><a href="https://github.com/ki-sd/SpringThymeLeafDockerProject.git" target="_blank"><img src="https://img.shields.io/badge/GitHub-181717?style=flat-square&logo=github&logoColor=white"></a></li><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[JPA 조건 검색 Repository]</strong> `RecipeRepository`에 `findByTitleContains`, `findByChefContains` 등 `Pageable`을 지원하는 메서드와 카운트 메서드를 추가함. <br><strong>[검색 비즈니스 로직 및 REST API]</strong> `RecipeServiceImpl`에서 검색 타입(`type`)과 검색어(`fd`)에 따른 페이징 및 총 페이지 수 연산 로직을 구현하고, `RecipeRestController`를 통해 비동기 검색 결과(`find_vue`)를 `ResponseEntity`로 응답함. <br><strong>[View 및 Vue 3 연동]</strong> `RecipeController`에 검색 페이지 라우팅을 추가하고, `chef.html`에서 특정 셰프 클릭 시 검색 화면으로 연동되도록 링크를 구성함. 또한 `find.html`에서 Vue 3와 Axios를 활용해 검색 및 페이징을 처리하는 동적 화면을 구현함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[JPA Conditional Search Repository]</strong> Added `Pageable`-supported query methods such as `findByTitleContains`, `findByChefContains`, and count methods to `RecipeRepository`. <br><strong>[Search Business Logic & REST API]</strong> Implemented pagination and total page calculation logic based on search type (`type`) and keyword (`fd`) in `RecipeServiceImpl`, and responded with asynchronous search results (`find_vue`) via `ResponseEntity` in `RecipeRestController`. <br><strong>[Views & Vue 3 Integration]</strong> Added search page routing to `RecipeController` and linked chef items in `chef.html` to direct users to the search view. Also implemented a dynamic search interface using Vue 3 and Axios in `find.html`.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[JPA条件検索Repository]</strong> `RecipeRepository`に`findByTitleContains`、`findByChefContains`などの`Pageable`をサポートするメソッドおよびカウントメソッドを追加。<br><strong>[検索ビジネスロジックとREST API]</strong> `RecipeServiceImpl`にて検索タイプ（`type`）と検索ワード（`fd`）に応じたページングおよび総ページ数演算ロジックを実装し、`RecipeRestController`を通じて非同期検索結果（`find_vue`）を`ResponseEntity`で応答。<br><strong>[ビューおよびVue 3連携]</strong> `RecipeController`に検索ページルーティングを追加し、`chef.html`で特定のシェフをクリックした際に検索画面へ連동されるようリンクを構成。また、`find.html`にてVue 3とAxiosを活用し、検索およびページング処理を行う動的画面を実装。</li></ul> |
| **08-10** | <ul><li><a href="https://github.com/ki-sd/SpringThymeLeafDockerProject.git" target="_blank"><img src="https://img.shields.io/badge/GitHub-181717?style=flat-square&logo=github&logoColor=white"></a></li><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[Spring File Upload]</strong> `UploadRestController` 및 `DataBoardController`에서 `MultipartFile`을 활용해 단일 및 다중 파일 업로드 로직을 구현함. 중복 파일명 덮어쓰기 방지 알고리즘과 `java.nio.file.Files`를 이용한 저장 처리 방식을 구축함. <br><strong>[MyBatis 기반 자료실 로직]</strong> `DataBoardMapper`에서 `@SelectKey`를 사용하여 파일 정보(`filename`, `filesize`, `filecount`)가 포함된 게시물 Insert 쿼리를 작성하고, 서비스 계층에서 레코드 수를 기반으로 하단 페이징 블록 알고리즘을 연산함. <br><strong>[Dynamic Front-end UI]</strong> 뷰(View) 렌더링 시 Vue 3(`FormData` + `Axios`)를 활용한 비동기 멀티 파일 전송 로직과, jQuery를 활용해 파일 입력 DOM을 동적으로 추가/삭제하는 UX를 구현함. <br><strong>[Thymeleaf 연동]</strong> `list.html`에서 `#numbers.sequence` 디렉티브를 이용한 페이징 처리와 `th:if="${vo.filecount>0}"`을 사용한 조건부 첨부파일 UI 렌더링을 구현함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Spring File Upload]</strong> Implemented single and multiple file upload logic using `MultipartFile` in `UploadRestController` and `DataBoardController`. Established an algorithm to prevent filename duplication and utilized `java.nio.file.Files` for robust file saving. <br><strong>[MyBatis DataBoard Logic]</strong> Wrote insert queries including file metadata (`filename`, `filesize`, `filecount`) using `@SelectKey` in `DataBoardMapper`, and computed pagination block algorithms in the service layer based on total record counts. <br><strong>[Dynamic Front-end UI]</strong> Developed asynchronous multi-file upload mechanisms using Vue 3 (`FormData` + `Axios`) and created an interactive UX with jQuery to dynamically append/remove file input DOM elements. <br><strong>[Thymeleaf Integration]</strong> Rendered dynamic pagination navigation using `#numbers.sequence` and implemented conditional UI elements for file attachments using `th:if="${vo.filecount>0}"` in `list.html`.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[Spring File Upload]</strong> `UploadRestController`および`DataBoardController`にて、`MultipartFile`を活用した単一および複数ファイルのアップロードロジックを実装。ファイル名の重複防止アルゴリズムと`java.nio.file.Files`を利用した保存処理を構築。<br><strong>[MyBatisデータボードロジック]</strong> `DataBoardMapper`で`@SelectKey`を使用し、ファイル情報（`filename`、`filesize`、`filecount`）を含む掲示板のInsertクエリを作成。サービス階層でレコード数に基づくページングブロックのアルゴリズムを演算。<br><strong>[Dynamic Front-end UI]</strong> Vue 3（`FormData` + `Axios`）を活用した非同期のマルチファイル送信ロジックと、jQueryを用いてファイル入力DOMを動的に追加・削除するUXを実装。<br><strong>[Thymeleaf連携]</strong> `list.html`において、`#numbers.sequence`ディレクティブを用いたページング処理と、`th:if="${vo.filecount>0}"`を利用した条件付き添付ファイルUIのレンダリングを実装。</li></ul> |
| **08-12** | <ul><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[Entity Mapping]</strong> `@ManyToOne`과 `@JoinColumn`을 활용해 사원(`Emp`)과 부서(`Dept`) 엔티티 간의 다대일 연관관계를 설정함. <br><strong>[JPA Method & JPQL]</strong> `EmpRepository`에서 메서드 네이밍 규칙(`findTop3By...` 등)을 통해 기본 조회를 처리하고, `EmpJpqlRepository`에서 `@Query`를 이용해 객체 중심 SQL인 JPQL로 복잡한 JOIN 및 조건 검색을 구현함. <br><strong>[QueryDSL Integration]</strong> `JPAQueryFactory`를 빈으로 등록하여 QueryDSL 환경을 세팅함. 자동 생성된 Q-Class(`QEmp`, `QDept`)를 활용해 컴파일 단계에서 오류를 잡을 수 있는 안전하고 유연한 동적 쿼리(메서드 체이닝) 환경을 `EmpQueryRepository`에 구축함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[Entity Mapping]</strong> Established a Many-To-One relationship between Employee (`Emp`) and Department (`Dept`) entities using `@ManyToOne` and `@JoinColumn`. <br><strong>[JPA Method & JPQL]</strong> Handled basic queries using method naming conventions in `EmpRepository`, and implemented complex JOINs and conditional searches with JPQL using the `@Query` annotation in `EmpJpqlRepository`. <br><strong>[QueryDSL Integration]</strong> Configured the QueryDSL environment by registering `JPAQueryFactory` as a Bean. Built a type-safe and flexible dynamic query environment in `EmpQueryRepository` using auto-generated Q-Classes (`QEmp`, `QDept`) and method chaining.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[Entity Mapping]</strong> `@ManyToOne`と`@JoinColumn`を活用し、社員（`Emp`）と部署（`Dept`）エンティティ間の多対一の関連関係を設定。<br><strong>[JPA Method & JPQL]</strong> `EmpRepository`にてメソッド命名規則（`findTop3By...`など）を利用して基本検索を処理し、`EmpJpqlRepository`で`@Query`を用いてオブジェクト指向SQLであるJPQLで複雑なJOINや条件検索を実装。<br><strong>[QueryDSL Integration]</strong> `JPAQueryFactory`をBeanとして登録し、QueryDSL環境を構築。自動生成されたQ-Class（`QEmp`、`QDept`）を活用し、コンパイル段階でエラーを検出できる安全で柔軟な動的クエリ（メソッドチェーン）環境を`EmpQueryRepository`に構築。</li></ul> |
| **08-13** | <ul><li><a href="https://github.com/ki-sd/SpringPiniaProject.git" target="_blank"><img src="https://img.shields.io/badge/GitHub-181717?style=flat-square&logo=github&logoColor=white"></a></li><li><img src="https://flagcdn.com/w20/kr.png" width="20"> <strong>[MyBatis 쿼리 최적화]</strong> `RecipeMapper.xml`에서 `INTERSECT` 연산자를 활용하여 레시피 상세 데이터가 존재하는 게시물만 조회되도록 무결성을 높이고, `${type}` 바인딩을 이용해 동적 검색 쿼리를 구축함. <br><strong>[REST API 데이터 가공]</strong> `RecipeRestController`에서 조리 과정(`foodmake`) 원본 문자열을 `StringTokenizer`와 `^` 구분자를 활용하여 조리법 텍스트(`mList`)와 이미지(`iList`) 배열로 정밀하게 분리해 프론트엔드로 전달함. <br><strong>[Pinia 상태 관리 저장소]</strong> Vue 3 공식 상태 관리 라이브러리인 Pinia(`defineStore`)를 도입함. `state`로 전역 변수를 선언하고, `getters`로 페이지네이션 배열을 연산하며, `actions`에서 Axios를 통한 비동기 서버 통신 및 상태 갱신을 캡슐화하는 아키텍처를 완성함.</li><li><img src="https://flagcdn.com/w20/us.png" width="20"> <strong>[MyBatis Query Optimization]</strong> Improved data integrity in `RecipeMapper.xml` by using the `INTERSECT` operator to fetch only recipes with existing detail records, and built dynamic search queries using `${type}` binding. <br><strong>[REST API Data Processing]</strong> Processed raw cooking instruction strings (`foodmake`) in `RecipeRestController` using `StringTokenizer` with the `^` delimiter, separating them into text (`mList`) and image (`iList`) arrays for the frontend. <br><strong>[Pinia State Management]</strong> Introduced Pinia (`defineStore`), the official Vue 3 state management library. Established an architecture by declaring global variables in `state`, computing pagination arrays in `getters`, and encapsulating asynchronous server communication via Axios and state updates in `actions`.</li><li><img src="https://flagcdn.com/w20/jp.png" width="20"> <strong>[MyBatis クエリ最適化]</strong> `RecipeMapper.xml`にて`INTERSECT`演算子を活用し、詳細データが存在するレシピのみ取得するようデータの整合性を高め、`${type}`バインディングを用いた動的検索クエリを構築。<br><strong>[REST API データ加工]</strong> `RecipeRestController`にて、調理手順（`foodmake`）の元の文字列を`StringTokenizer`と`^`区切り文字を利用し、手順テキスト（`mList`）と画像（`iList`）の配列に精密に分離してフロントエンドへ伝達。<br><strong>[Pinia 状態管理ストア]</strong> Vue 3公式の状態管理ライブラリであるPinia（`defineStore`）を導入。`state`でグローバル変数を宣言し、`getters`でページネーション配列を演算し、`actions`内でAxiosによる非同期サーバー通信および状態更新をカプセル化するアーキテクチャを完成。</li></ul> |